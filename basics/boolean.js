// === equality operator
// !== not equal operator
// < less than operator
// > greater then operator

let temp = 31;
let isFreezing = temp < 32;

console.log(isFreezing);

if (isFreezing) {
    console.log('it is freezing');
}